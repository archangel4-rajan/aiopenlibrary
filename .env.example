# ============================================
# AIOpenLibrary — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values.
# The app works without Supabase (falls back to hardcoded data),
# but auth, saving, and submissions require a live Supabase project.

# Supabase project URL (public — safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous / public key (public — safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (SECRET — server-side only, used for seeding)
# WARNING: Never expose this in client-side code or commit it to the repo
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
